# Figure reproduction

This directory contains scripts for regenerating figures used in the AMS submission.

## Script

- `plot_diurnal_cycle_svg.py`:
  - Input CSV columns: `hour_utc,mean_value,count`
  - Output: SVG line plot of mean value vs UTC hour
